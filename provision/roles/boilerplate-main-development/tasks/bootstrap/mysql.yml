---

- name: Deploy custom mysql-docker.cnf
  template:
#    src:   templates/mysql-docker.cnf.j2
    src:   "/opt/docker/etc/mysql/mysql-{{ PROJECT_NAME|lower }}.cnf"
    dest:  "/etc/mysql/conf.d/z99-docker.cnf"
    owner: root
    group: root
    mode:  0644

#- name: Deploy user my.cnf
#  template:
#    src:   templates/my.cnf.j2
#    dest:  /home/.my.cnf
#    owner: "{{ lookup('env','APPLICATION_USER') }}"
#    group: "{{ lookup('env','APPLICATION_USER') }}"
#    mode:  0644
